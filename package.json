{
  "name": "corner",
  "version": "0.0.1",
  "description": "corner template",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:client": "cd client && yarn install",
    "install:server": "cd server && yarn install",
    "install:all": "yarn install:client && yarn install:server",
    "build:client": "cd client && yarn build",
    "build:server": "cd server && yarn build:server",
    "build:all": "yarn build:client && yarn build:server",
    "start": "yarn build:all && cd server && yarn run:server"
  },
  "author": "ghostcorn",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
}
